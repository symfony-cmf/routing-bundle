<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <services>
        <!-- @TODO: Support driver chain (i.e. more than Annotation driver) !-->
        <!-- <service 
            id="symfony_cmf_routing_extra.driver_chain" 
            class="Metadata\Driver\DriverChain"
            >
        </service>
        <!-->

        <service 
            id="symfony_cmf_routing_extra.annotation_driver" 
            public="false"
            class="Symfony\Cmf\Bundle\RoutingExtraBundle\Mapping\Driver\AnnotationDriver"
            >
            <argument type="service" id="annotation_reader"/>
        </service>

        <service
            id="symfony_cmf_routing_extra.metadata_factory"
            class="Metadata\MetadataFactory"
            >
            <argument type="service" id="symfony_cmf_routing_extra.annotation_driver"/>
            <!-- @TODO: Netvlies annotation driver specifies a cache, but they also use
                 they're own Annotation Driver, do we need a cache? !-->
        </service>

    </services>
</container>

